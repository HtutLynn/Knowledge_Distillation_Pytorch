# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]

# pytorch framework trained models
*.pt
*.pth
model_info.txt

# datasets are stored in dataset folder
# since datasets are heavy in terms of size, we are going to ignore everything inside that folder
datasets/*
!datasets/__init__.py # exception to the rule since it keeps the folder

# Visual Studio Code files
.vscode
.vs

# C extensions
*.so

# Distribution / packaging
bin/
build/
develop-eggs/
dist/
eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*,cover

# Translations
*.mo
*.pot

# Django stuff:
*.log

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# macOS dir files
.DS_Store

# Files generated when a patch is rejected
*.orig
*.rej
